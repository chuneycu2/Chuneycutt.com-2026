export default function SkillsAndTools() {
    return (
        <div className="skillset entry">
            {/* programmatically map through props to render each experience */}
        </div>
    )
}